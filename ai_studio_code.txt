{
  "type": "function",
  "function": {
    "name": "analyze_crypto_market",
    "description": "获取 Bitget 交易所中指定加密货币的 K线数据，并返回行情基础面和技术指标分析（如价格、涨跌幅、MA均线）。",
    "parameters": {
      "type": "object",
      "properties": {
        "symbol": {
          "type": "string",
          "description": "交易对名称，必须是大写字母并且不包含特殊字符。例如：'BTCUSDT', 'ETHUSDT'"
        },
        "timeframe": {
          "type": "string",
          "enum": ["1min", "5min", "15min", "30min", "1h", "4h", "12h", "1day", "1week"],
          "description": "K线的时间粒度（周期），默认为 1h（1小时）"
        },
        "limit": {
          "type": "integer",
          "description": "获取的历史 K 线数量，建议 50 到 100",
          "default": 50
        }
      },
      "required": ["symbol"]
    }
  }
}